@echo off
REM Example user settings for WebUI Codex (Windows)
REM Copy this file to webui.settings.bat and edit values as needed.

REM Logging level for backend (used by backend/logging_utils.py)
REM Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
set CODEX_LOG_LEVEL=DEBUG

REM Optional: write logs to a file in addition to console
REM set CODEX_LOG_FILE=.webui-codex.log

REM You can also use SDWEBUI_LOG_LEVEL or WEBUI_LOG_LEVEL if preferred
REM set SDWEBUI_LOG_LEVEL=INFO

REM --- Dtype preferences (optional) ---
REM Force UNet dtype: bf16 | fp16 | fp32 | fp8_e4m3fn | fp8_e5m2
set CODEX_UNET_DTYPE=bf16
REM Force VAE dtype: bf16 | fp16 | fp32
set CODEX_VAE_DTYPE=bf16
REM Place VAE on CPU (optional)
REM set CODEX_VAE_IN_CPU=0
REM Force everything to fp32 (overrides heuristics)
REM set CODEX_ALL_IN_FP32=0
